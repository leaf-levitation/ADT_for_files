假设 $\Gamma = \{z(t); a \le t \le b\}$ 是一条拟简单曲线。如果 $M^*(\Gamma) < \infty$，则该曲线是可求长的，如果 $L$ 表示其长度，则
$$ L \le M_*(\Gamma) $$
该证明依赖于以下简单的观察。

假设 $\Gamma = \{z(t); a \le t \le b\}$ 是一条拟简单曲线。如果 $M^*(\Gamma) < \infty$，则该曲线是可求长的，如果 $L$ 表示其长度，则
$$ L \le M_*(\Gamma) $$
该证明依赖于以下简单的观察。

